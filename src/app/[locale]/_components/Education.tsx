import Section from "components/Section";

export type EducationProps = {};

const Education = ({ ...props }: Readonly<EducationProps>) => {
  return <Section id="education" title="Education"></Section>;
};

export default Education;
